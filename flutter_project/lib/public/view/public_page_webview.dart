import 'package:flutter/material.dart';

class PublicPageWebView extends StatelessWidget {
  const PublicPageWebView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
